package Configuration;

/**
 * # Entities.Entity
 * Handles objects that can move (train carts and the pusher cart).
 **/
public class jsonEntity {
    public int x;
    public int y;
    public int trainNumber = -1;
    public int direction = -1;
}
